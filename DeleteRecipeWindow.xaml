<Window x:Class="StudentCookbook.DeleteRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Usuń przepis"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Usuń przepis"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryTextBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,20">
            <Label Content="Wybierz przepis do usunięcia:"
                   Foreground="{DynamicResource PrimaryTextBrush}"/>
            <ComboBox x:Name="RecipesComboBox" Width="300" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="❌ Usuń" 
                    Width="120"
                    Height="40"
                    Margin="5"
                    Background="{DynamicResource RedButtonBrush}" 
                    Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>